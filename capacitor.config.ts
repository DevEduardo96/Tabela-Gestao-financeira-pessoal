import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.financ.app",
  appName: "sistem-finac",
  webDir: "dist/public", // <--- ALTERE AQUI
};

export default config;
